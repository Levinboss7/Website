# Website

This project contains a simple image gallery website. The gallery loads a set of example images from Unsplash.

## Usage

Open `index.html` in your browser. The page displays a grid of images loaded from the web. You can replace the URLs listed in `app.js` with your own image URLs if you want to customize the content.
